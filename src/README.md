```
├── cockpit.js  <- main application
├── lib         <- for node.js modules
├── plugins     <- plugins that create the behavior for cockpit
├── static      <- global html and js files
├── system-plugins  <- plugins for the cockpit infrastructure 
└── tests       <- node.js tests
```